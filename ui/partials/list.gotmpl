{{define "list"}}
<div class="list">
  {{range .Site.Posts}}
  <div class="list-item">
    <a href="{{.Slug}}">
      <div class="list-content">
        <h2>{{.Title}}</h2>
        <h3>{{.TagString}}</h3>
        <p>{{.Summary}}</p>
      </div>
    </a>
  </div>
  {{end}}
</div>
{{end}}
